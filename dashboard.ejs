<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Ana Sayfa</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <div class="max-w-6xl mx-auto px-6 py-10">
    <div class="flex justify-between items-center mb-8">
      <h1 class="text-3xl font-bold text-gray-800">Merhaba, <%= user.name %> 👋</h1>
      <a href="/logout" class="text-red-600 font-medium hover:underline">Çıkış Yap</a>
    </div>

    <!-- İstatistik kutuları -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
      <div class="bg-white p-6 rounded-lg shadow text-center">
        <h2 class="text-xl font-semibold text-gray-700">Toplam Araç</h2>
        <p class="text-2xl font-bold text-blue-600"><%= cars.length %></p>
      </div>
      <div class="bg-white p-6 rounded-lg shadow text-center">
        <h2 class="text-xl font-semibold text-gray-700">Kiralanan Araçlar</h2>
        <p class="text-2xl font-bold text-yellow-500">
          <%= cars.filter(car => car.status === 'Kiralandı').length %>
        </p>
      </div>
      <div class="bg-white p-6 rounded-lg shadow text-center">
        <h2 class="text-xl font-semibold text-gray-700">Müsait Araçlar</h2>
        <p class="text-2xl font-bold text-green-500">
          <%= cars.filter(car => car.status === 'müsait').length %>
        </p>
      </div>
    </div>

    <!-- Araç listesi -->
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      <% cars.forEach(car => { %>
        <div class="bg-white rounded-lg shadow-md p-4">
          <h3 class="text-lg font-bold"><%= car.brand %> <%= car.model %></h3>
          <p class="text-sm text-gray-600">Fiyat: <%= car.price %>₺</p>
          <p class="text-sm text-gray-600">Motor: <%= car.engine_size || '-' %></p>
          <p class="text-sm text-gray-600">Yıl: <%= car.year || '-' %></p>
          <p class="text-sm mt-1 font-medium text-gray-700">
            Durum:
            <span class="<%= car.status === 'müsait' ? 'text-green-600' : 'text-red-500' %>">
              <%= car.status %>
            </span>
          </p>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>